node_modules
.next
next-env.d.ts
*.tsbuildinfo
.env
.env.local

# WhatsApp session + local data (contains your login — never commit)
.wwebjs_auth
.wwebjs_cache
data/db.json

*.log
.DS_Store

# Local Claude Code / preview config — not needed in the repo
.claude/
.preview-wwebjs/
.preview-data/
# Windows folder-icon cosmetics - local only, not part of the project
desktop.ini
folder-icon.ico
